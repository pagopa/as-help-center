/**
 * @namespace HiddenArticles
 */
const HiddenArticles = {
  /**
   * Get ids array of hidden articles
   * @memberof HiddenArticles
   * @returns {Array} hiddenIds
   */
  getIds() {
    const hiddenIds = [35541811236113];
    return hiddenIds;
  },

  /**
   * Get titles array of hidden articles
   * @memberof HiddenArticles
   * @returns {Array} hiddenTitles
   */
  getTitles() {
    const hiddenTitles = ['Cosa serve per usare IT-Wallet'];
    return hiddenTitles;
  }
};

window.HiddenArticles = HiddenArticles;

/**
 * @namespace HiddenSections
 */
const HiddenSections = {
  /**
   * Get ids array of hidden sections
   * @memberof HiddenSections
   * @returns {Array} hiddenIds
   */
  getIds() {
    const hiddenIds = [36576602674577];
    return hiddenIds;
  },

  /**
   * Get titles array of hidden sections
   * @memberof HiddenSections
   * @returns {Array} hiddenTitles
   */
  getTitles() {
    const hiddenTitles = ['IT-Wallet'];
    return hiddenTitles;
  }
};

window.HiddenSections = HiddenSections;
